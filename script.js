const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const menuBtn=$("#menuBtn"),mobileMenu=$("#mobileMenu"),closeMenu=$("#closeMenu"),scrim=$("#scrim");
function openMenu(){mobileMenu.classList.add("open");scrim.classList.add("open");menuBtn.setAttribute("aria-expanded","true")}function closeMenuFn(){mobileMenu.classList.remove("open");scrim.classList.remove("open");menuBtn.setAttribute("aria-expanded","false")}menuBtn.addEventListener("click",openMenu);closeMenu.addEventListener("click",closeMenuFn);scrim.addEventListener("click",closeMenuFn);$$('.mobile-menu a').forEach(a=>a.addEventListener('click',closeMenuFn));
addEventListener('scroll',()=>{const d=document.documentElement,p=d.scrollTop/(d.scrollHeight-d.clientHeight)*100;$('#progressBar').style.width=Math.max(0,Math.min(100,p))+'%'});
const modal=$("#demoModal");function closeModal(){modal.classList.remove('open');modal.setAttribute('aria-hidden','true')}$$('[data-demo]').forEach(b=>b.onclick=()=>modal.classList.add('open'));$('#modalClose').onclick=closeModal;modal.onclick=e=>{if(e.target===modal)closeModal()};document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeModal();closeMenuFn()}});
const toast=$('#toast');$$('[data-download]').forEach(b=>b.onclick=e=>{e.preventDefault();toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),2400)});
$$('details').forEach(d=>d.addEventListener('toggle',()=>{if(d.open)$$('details').filter(x=>x!==d).forEach(x=>x.removeAttribute('open'))}));
if(!matchMedia('(prefers-reduced-motion: reduce)').matches){const obs=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('revealed');obs.unobserve(e.target)}}),{threshold:.08});$$('.lux-card,.section-label,.intro-grid>*').forEach(e=>{e.classList.add('reveal');obs.observe(e)});const hero=$('.hero'),logo=$('.hero-logo-wrap'),card=$('.hero-card-float');hero.addEventListener('pointermove',e=>{const r=hero.getBoundingClientRect(),x=e.clientX/r.width-.5,y=e.clientY/r.height-.5;logo.style.transform=`translate(${x*7}px,${y*7}px)`;if(card)card.style.transform=`translate(${x*-12}px,${y*-8}px)`});hero.addEventListener('pointerleave',()=>{logo.style.transform='';if(card)card.style.transform=''})}
